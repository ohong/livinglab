// corresponds to the create action in messages_controller
// jQuery script for some sweet sweet AJAX!
// the form's ID by default is #new_message
// clear text area after sending message
$('#chat').prepend("<%= j render @message %>");
$('#new_message')[0].reset();
